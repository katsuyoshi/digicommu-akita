- @title = 'スケジュール'

%div#main_title
  %h1 デジコミュ秋田 2012

%div#schedule{name:'schedule'}
  %h2 スケジュール
  %h3 2012年11月10日土曜日

  %table
    - @schdules.each do |schedule|
      %tr{ 'class' => schedule.kind }
        %td= "#{schedule.start} - " 
        %td
          - if schedule.has_link?
            = link_to schedule.title, schedule_show_path(:id => schedule.id)
          - else
            = schedule.title
